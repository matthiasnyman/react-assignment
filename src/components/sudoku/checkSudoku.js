

const CheckSudoku = (input, array) => {
  const rows = array.rows[input.row].cols;
  // rows.map(x => {
  //   if(value === input.value ){
  //     console.log(input);
  //   }
  // })
  
}

export default CheckSudoku;